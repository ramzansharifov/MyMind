function App() {
  return (
    <main className="min-h-screen bg-slate-100 text-slate-950">
      <section className="flex min-h-screen items-center justify-center p-6">
        <div className="w-full max-w-3xl rounded-3xl border border-slate-200 bg-white p-8 shadow-xl">
          <p className="mb-3 text-sm font-semibold uppercase tracking-[0.25em] text-violet-600">
            Multifunctional Board
          </p>

          <h1 className="text-4xl font-bold tracking-tight">
            Tailwind подключён успешно
          </h1>

          <p className="mt-4 text-lg leading-8 text-slate-600">
            Теперь можно проектировать полноценную доску: панели инструментов,
            рабочую область, карточки, фигуры, заметки, связи, слои и сохранение данных.
          </p>

          <div className="mt-8 flex flex-wrap gap-3">
            <button className="rounded-2xl bg-violet-600 px-5 py-3 font-semibold text-white shadow-lg shadow-violet-600/20 transition hover:bg-violet-700">
              Создать доску
            </button>

            <button className="rounded-2xl border border-slate-300 bg-white px-5 py-3 font-semibold text-slate-700 transition hover:bg-slate-50">
              Открыть проект
            </button>
          </div>
        </div>
      </section>
    </main>
  )
}

export default App
